name: "Build Project"
on:
  push:
    branches:
      - '**'
  
jobs:
  build:
    uses: ./.github/workflows/generic_build.yml
  upload-artifact:
    name: "Upload Artifact"
    uses: actions/upload-artifact@v7
    with:
      path: .
